[build]
  command   = "npm run build"
  publish   = "dist/halal-box-jo/browser"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — serve index.html for every route
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
